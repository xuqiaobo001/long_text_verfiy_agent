# 长文本审核Agent

一个专为长文本设计的智能审核Agent，支持合同审核、传媒稿件审核、学术论文审核等多种场景。通过智能文本分割和并行处理，有效解决了大模型的上下文限制问题。

[![Python Version](https://img.shields.io/badge/python-3.8+-blue.svg)](https://python.org)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Status](https://img.shields.io/badge/status-production--ready-brightgreen.svg)]()

## ✨ 特性

- 🚀 **高性能**: 支持MB级长文本的快速审核
- 📄 **多格式支持**: PDF、Word(DOCX)、TXT、Markdown、HTML等格式
- 🎯 **多场景**: 内置合同、传媒、学术等多种审核场景
- 🔍 **智能分割**: 按章节自动分割文本，保持上下文关联
- ⚡ **并行处理**: 多文本块并行审核，提升处理效率
- 🔗 **一致性检查**: 跨章节的一致性检查，确保全文统一
- 🎛️ **可配置**: 所有配置项均可通过配置文件调整
- 📊 **详细报告**: 生成结构化的审核报告和改进建议

## 🏗️ 架构设计

### 系统架构图

```
┌─────────────────┐
│   输入文件      │
│ (docx/pdf/txt)  │
└─────────┬───────┘
          │
          ▼
┌─────────────────┐
│   文件读取层     │
│ TextSplitter    │
└─────────┬───────┘
          │
          ▼
┌─────────────────┐
│   智能分割层     │
│ Chapter/Paragraph│
│ /Semantic/Fixed  │
└─────────┬───────┘
          │
          ▼
┌─────────────────┐
│   并行处理层     │
│ ChunkProcessor  │
└─────────┬───────┘
          │
          ▼
┌─────────────────┐
│   AI审核层      │
│  华为云MAAS     │
│   DeepSeek-V3   │
└─────────┬───────┘
          │
          ▼
┌─────────────────┐
│   结果聚合层     │
│ Consistency     │
│   Checker       │
└─────────┬───────┘
          │
          ▼
┌─────────────────┐
│   输出报告      │
│ (Excel/JSON)    │
└─────────────────┘
```

### 核心模块

- **配置管理** (`src/config/`): 动态配置加载，环境变量支持
- **模型接口** (`src/models/`): MAAS API客户端，多格式文件处理
- **核心审核** (`src/core/`): 长文本审核逻辑，一致性检查
- **工具模块** (`src/utils/`): 文件处理，日志管理

## 🚀 快速开始

### 环境要求

- Python 3.8+
- 华为云MAAS API访问权限

### 📦 安装

详细的安装说明请参考 [**INSTALL.md**](INSTALL.md)

**快速安装步骤**：
```bash
# 1. 克隆项目
git clone <repository-url>
cd long_text_review

# 2. 创建虚拟环境
python3 -m venv venv
source venv/bin/activate  # Linux/Mac

# 3. 安装依赖
pip install -r requirements.txt

# 4. 配置API密钥
echo "MAAS_API_KEY=your_api_key" > .env

# 5. 验证安装
python install_check.py
```

### 基本使用

#### 命令行方式

```bash
# 审核文本文件
python main.py input.txt -s contract -o result.json

# 指定场景和输出格式
python main.py contract.docx -s contract --format yaml

# 提供上下文信息
python main.py document.txt --context "这是一份软件开发合同"

# 直接审核文本内容
python main.py "待审核文本" --text
```

#### Python API方式

```python
import asyncio
from src.core.reviewer import LongTextReviewer

async def review_text():
    # 创建审核器
    async with LongTextReviewer(scenario="contract") as reviewer:
        # 审核文件
        result = await reviewer.review_file("contract.docx")

        # 获取结果
        print(f"评分: {result.overall_score}")
        print(f"问题数: {len(result.issues)}")

        # 保存结果
        reviewer.save_result(result, "output.json")

asyncio.run(review_text())
```

## 📚 审核场景

### 1. 合同审核 (`contract`)

专门用于审核各类合同文档：

- **法律条款完整性**: 违约责任、争议解决、生效条件等
- **主体信息一致性**: 各方名称、地址、法定代表人等
- **权利义务对等性**: 双方责任是否平衡
- **条款冲突检测**: 识别条款间的矛盾
- **义务可执行性**: 确保义务条款具体可执行

### 2. 传媒稿件审核 (`media`)

用于新闻、文章等传媒内容：

- **事实准确性**: 数据、日期、名称等核实
- **偏见检测**: 识别潜在的立场偏差
- **敏感内容识别**: 政治、宗教、种族等敏感词
- **信息来源可靠性**: 核实引用来源
- **标题与内容一致性**: 确保标题准确反映内容

### 3. 学术论文审核 (`academic`)

用于学术论文、研究报告：

- **结构完整性**: 摘要、引言、方法、结果、讨论等
- **引用一致性**: 文内引用与参考文献匹配
- **方法描述清晰度**: 确保研究可复现
- **数据完整性**: 数据展示的准确性
- **术语使用规范**: 学术术语的一致性

### 4. 通用审核 (`general`)

适用于一般文档的基础审核：

- **格式规范**: 排版、格式标准化
- **错别字检查**: 文字错误识别
- **标点符号**: 标点使用规范
- **逻辑连贯性**: 内容逻辑清晰
- **术语一致性**: 全文术语统一

## 📖 文本分割策略

### 章节分割（推荐）
- 自动识别章节标题
- 保持文档逻辑结构
- 适用于结构化文档

### 段落分割
- 按空行分割
- 保持段落完整性
- 适用于散文、小说等

### 语义分割
- 基于内容相关性
- 智能合并相关段落
- 适用于复杂文档

### 固定大小分割
- 按字符数分割
- 可配置重叠大小
- 适用于无结构文档

## 🔍 一致性检查

系统自动进行以下一致性检查：

### 术语一致性
- 检查全文术语使用是否统一
- 识别不一致的表达
- 建议标准化表述

### 事实一致性
- 核对数据、日期、名称的一致性
- 跨章节信息验证
- 识别矛盾信息

### 逻辑一致性
- 检查逻辑连接是否清晰
- 识别逻辑跳跃
- 建议改善过渡

### 需求一致性（合同场景）
- 检查需求定义是否无冲突
- 识别相互矛盾的条款
- 确保条款协调统一

## 📊 输出格式

### JSON格式
```json
{
  "overall_score": 85.5,
  "issues": [
    {
      "chunk_id": 0,
      "review_point": "法律条款完整性",
      "type": "completeness",
      "severity": "high",
      "description": "缺少违约责任条款",
      "location": "第三章",
      "suggestion": "建议添加违约责任相关条款"
    }
  ],
  "issues_by_severity": {
    "critical": [],
    "high": [1],
    "medium": [2],
    "low": [0]
  },
  "suggestions": ["建议完善合同的法律条款"],
  "summary": "整体质量良好，但需要完善部分条款",
  "consistency_results": {
    "consistency_score": 90.0
  }
}
```

### Excel格式
生成详细的表格报告，包含：
- 审核要点清单
- 风险等级分类
- 章节位置标注
- 具体修改建议

## ⚙️ 配置说明

### 主配置文件 (`config/config.yaml`)

```yaml
# 华为云MAAS配置
maas:
  base_url: "https://api.modelarts-maas.com/v1"
  model: "DeepSeek-V3"
  api_key: "${MAAS_API_KEY}"
  timeout: 120
  max_retries: 3

# 文本处理配置
text_processing:
  chunking:
    strategy: "chapter"  # chapter/paragraph/semantic/fixed_size
    max_chunk_size: 8000
    chunk_overlap: 200
    min_chunk_size: 1000

# 审核配置
review:
  parallel:
    max_workers: 4
    enable_parallel: true
  result:
    output_format: "json"
    save_intermediate: true
```

### 审核点配置 (`config/review_points.yaml`)

```yaml
review_points:
  contract:
    legal_terms:
      - name: "法律条款完整性"
        description: "检查必要的法律条款是否完整"
        type: "completeness"
        priority: "critical"
        enabled: true
```

## 📝 使用示例

### 合同审核示例

```python
import asyncio
from src.core.reviewer import LongTextReviewer

async def review_contract():
    async with LongTextReviewer(scenario="contract") as reviewer:
        result = await reviewer.review_file("contract.docx")

        # 输出结果
        issues_by_severity = result.get_issues_by_severity()

        print(f"整体评分: {result.overall_score}/100")
        print(f"严重问题: {len(issues_by_severity['critical'])}个")
        print(f"重要问题: {len(issues_by_severity['high'])}个")

        # 保存详细报告
        reviewer.save_result(result, "contract_review.xlsx", "excel")
```

### 批量处理示例

```python
import asyncio
from pathlib import Path

async def batch_review():
    files = Path("contracts").glob("*.docx")

    async with LongTextReviewer(scenario="contract") as reviewer:
        for file in files:
            result = await reviewer.review_file(str(file))
            output = f"output/{file.stem}_review.json"
            reviewer.save_result(result, output)
            print(f"已审核: {file}")

asyncio.run(batch_review())
```

## 🧪 测试

### 运行测试

```bash
# 运行所有测试
python -m pytest tests/

# 运行特定测试
python tests/test_reviewer.py

# 生成覆盖率报告
pytest --cov=src tests/
```

### 测试文件

- `tests/test_reviewer.py`: 核心功能测试
- `test_file/contract_review_points.txt`: 合同审核示例输出

## 🔧 性能优化

### 处理能力

- **文件大小**: 理论上无限制（已测试50MB+）
- **处理速度**: 1-3秒/MB
- **并行度**: 可配置1-8个worker
- **内存占用**: 流式处理，峰值约为文件大小的2-3倍

### 优化建议

1. **合理配置chunk大小**
   - 8000字符是较好平衡点
   - 简单文档可增大到12000
   - 复杂文档建议6000

2. **调整并行度**
   - CPU核心数决定最佳worker数
   - 网络延迟高时适当减少

3. **启用缓存**
   - 重复文档审核可大幅提速
   - Redis缓存适合生产环境

## 🔮 扩展开发

### 添加新审核场景

1. 在`config/review_points.yaml`添加场景配置
2. 实现场景特定的审核点
3. 添加相应的测试用例

```yaml
# 新场景配置
patent:
  required_checks:
    - "patent_claims"
    - "novelty_assessment"
    - "prior_art"
```

### 自定义分割策略

```python
from src.models.text_splitter import TextSplitter

class CustomSplitter(TextSplitter):
    def _split_by_custom(self, text: str):
        # 实现自定义分割逻辑
        pass
```

### 集成其他AI模型

```python
from src.models.maas_client import MaaSClient

class CustomAIClient(MaaSClient):
    def chat(self, messages, **kwargs):
        # 实现自定义API调用
        pass
```

## ❓ 常见问题

### Q: 如何处理超大文件？
A: 系统自动进行智能分割，支持任意大小文件。建议：
- 配置合适的chunk_size（8000-12000）
- 启用并行处理
- 监控内存使用

### Q: API调用失败怎么办？
A: 系统内置重试机制。可以：
- 调整重试次数和延迟
- 检查API配额
- 设置超时时间

### Q: 如何提高审核准确性？
A: 建议：
- 提供详细的上下文信息
- 选择合适的审核场景
- 启用一致性检查
- 根据需要调整审核点

### Q: 支持哪些文件格式？
A: 支持格式：
- 文档: DOCX, PDF, TXT, MD
- 网页: HTML
- 其他: 可扩展支持

## 📄 许可证

本项目采用 [MIT许可证](LICENSE)。

## 🤝 贡献指南

1. Fork 项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📞 联系方式

- **Issues**: [GitHub Issues](https://github.com/your-repo/issues)
- **Email**: your-email@example.com
- **文档**: [Wiki](https://github.com/your-repo/wiki)

## 🙏 致谢

- 华为云MAAS提供的AI服务支持
- DeepSeek-V3模型的高性能表现
- 所有贡献者的努力

---

## 📚 文档索引

- **[INSTALL.md](INSTALL.md)** - 详细安装指南
- **[QUICKSTART.md](QUICKSTART.md)** - 快速开始指南
- **[GITIGNORE_GUIDE.md](GITIGNORE_GUIDE.md)** - Git忽略规则说明

## 📈 更新日志

### v1.0.0 (2025-12-12)
- ✨ 初始版本发布
- 🎯 支持4种审核场景
- 📊 完整的审核报告生成
- 🔧 可配置的审核点系统
- 🚀 高性能并行处理架构
- 📦 完整的安装和配置文档